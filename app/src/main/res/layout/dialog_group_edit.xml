<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="20dp" android:paddingEnd="20dp"
    android:paddingTop="8dp" android:paddingBottom="8dp">

    <!-- 그룹명 -->
    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:text="그룹명" android:textSize="13sp" android:textColor="@color/text_hint"
        android:layout_marginBottom="4dp" />
    <EditText android:id="@+id/groupEditName"
        android:layout_width="match_parent" android:layout_height="52dp"
        android:hint="예: 앞밭 1구역" android:textSize="16sp"
        android:textColor="@color/text_primary" android:textColorHint="@color/text_hint"
        android:background="@drawable/bg_input_normal"
        android:paddingStart="14dp" android:paddingEnd="14dp"
        android:inputType="text" android:singleLine="true"
        android:layout_marginBottom="14dp" />

    <!-- 밸브 선택 -->
    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:text="밸브 선택" android:textSize="13sp" android:textColor="@color/text_hint"
        android:layout_marginBottom="4dp" />
    <TextView android:id="@+id/groupEditNodeBtn"
        android:layout_width="match_parent" android:layout_height="52dp"
        android:gravity="center_vertical"
        android:hint="밸브 선택 (필수)"
        android:textSize="15sp" android:textColor="@color/text_hint"
        android:background="@drawable/bg_input_normal"
        android:paddingStart="14dp" android:paddingEnd="14dp"
        android:layout_marginBottom="8dp"
        android:clickable="true" android:focusable="true" />

</LinearLayout>
